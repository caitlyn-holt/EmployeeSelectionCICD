<Window x:Class="EmployeeSelection.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Логин -->
        <Label Grid.Row="0" Grid.Column="0" Content="Логин:" VerticalAlignment="Center"/>
        <TextBox x:Name="txtUsername" Grid.Row="0" Grid.Column="1" Margin="0,5" Height="30"/>

        <!-- Пароль -->
        <Label Grid.Row="1" Grid.Column="0" Content="Пароль:" VerticalAlignment="Center"/>
        <PasswordBox x:Name="txtPassword" Grid.Row="1" Grid.Column="1" Margin="0,5" Height="30"/>

        <!-- Индикатор надёжности пароля -->
        <Label x:Name="lblPasswordStrength" Grid.Row="2" Grid.Column="1" Content="" 
               Foreground="Orange" FontSize="10" Margin="0,0,0,5"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,20">
            <Button x:Name="btnLogin" Content="Войти" Width="100" Height="35" 
                    Margin="0,0,10,0" Background="LightGreen" Click="BtnLogin_Click"/>
            <Button x:Name="btnRegister" Content="Регистрация" Width="100" Height="35" 
                    Click="BtnRegister_Click"/>
        </StackPanel>

        <!-- Сообщение об ошибке -->
        <Label x:Name="lblError" Grid.Row="4" Grid.ColumnSpan="2" Content="" 
               Foreground="Red" HorizontalAlignment="Center" Margin="0,10"/>
    </Grid>
</Window>